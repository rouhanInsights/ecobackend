// const { verifyToken, requireAdmin } = require('../middlewares/authMiddleware');
// const express = require('express');
// const router = express.Router();
// const {
//     createProduct,
//     getAllProducts,
//     getProductById,
//     deleteProduct
//   } = require('../controllers/productController');
  
//   router.get('/products/:id',verifyToken, requireAdmin, getProductById);
// router.get('/products', verifyToken, requireAdmin, getAllProducts);
// router.post('/products', verifyToken, requireAdmin, createProduct);
// router.delete('/products/:id', verifyToken, requireAdmin, deleteProduct);